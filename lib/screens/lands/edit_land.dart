import 'package:flutter/material.dart';

class EditLandScreen extends StatelessWidget {
  const EditLandScreen({super.key, required land});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
